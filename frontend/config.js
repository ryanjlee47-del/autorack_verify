// Where the API lives. Empty = same origin (local dev, single-host deploys).
// The Cloudflare Pages build (build.sh) overwrites this from AUTORACK_API_BASE.
window.AUTORACK_CONFIG = { apiBase: "" };
